const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:3000/api/v1'
export async function api<T>(path: string, options: RequestInit = {}): Promise<T> { const response = await fetch(`${API_URL}${path}`, { headers: { 'Content-Type': 'application/json', ...(options.headers ?? {}) }, ...options }); if (!response.ok) { const body = await response.json().catch(() => null); throw new Error(body?.message?.[0] ?? body?.message ?? 'ارتباط با سرور ناموفق بود.') }; return response.status === 204 ? undefined as T : response.json() as Promise<T> }
